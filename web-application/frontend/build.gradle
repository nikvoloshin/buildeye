plugins {
    id "com.moowork.node" version "1.3.1"
}

task clean(type: Delete) {
    delete 'build'
}

task dev(type: NpmTask) {
    args = ['run', 'start']
}

task build(type: NpmTask) {
    args = ['run', 'build']
}
